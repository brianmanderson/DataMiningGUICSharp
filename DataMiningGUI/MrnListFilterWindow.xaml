<Window x:Class="DataMiningGUI.MrnListFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MRN List Filter" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        MinHeight="350" MinWidth="350">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4A90D9" Padding="15,10">
            <StackPanel>
                <TextBlock Text="MRN List Filter" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock Text="Paste one MRN per line. Only patients whose MRN appears in this list will be shown." 
                           FontSize="11" Foreground="#E0E0E0" Margin="0,5,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Paste / Load controls -->
        <Border Grid.Row="1" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="Paste from Clipboard" 
                        Click="PasteFromClipboard_Click"
                        Padding="10,5" Margin="0,0,5,0"
                        Background="#4CAF50" Foreground="White"/>
                <Button Content="Load from File..." 
                        Click="LoadFromFile_Click"
                        Padding="10,5" Margin="0,0,5,0"/>
                <Button Content="Clear" 
                        Click="Clear_Click"
                        Padding="10,5"/>
                <TextBlock x:Name="MrnCountText" Text="0 MRNs" 
                           VerticalAlignment="Center" Margin="15,0,0,0"
                           FontWeight="SemiBold" Foreground="#333"/>
            </StackPanel>
        </Border>

        <!-- MRN Text Area -->
        <TextBox x:Name="MrnTextBox" 
                 Grid.Row="2" 
                 AcceptsReturn="True" 
                 AcceptsTab="False"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="13"
                 Margin="10"
                 Padding="5"
                 TextWrapping="NoWrap"
                 TextChanged="MrnTextBox_TextChanged"/>

        <!-- Status -->
        <Border Grid.Row="3" Background="#FFF3E0" BorderBrush="#FFB74D" BorderThickness="0,1" Padding="12,8">
            <TextBlock x:Name="StatusText" 
                       Text="Paste or type MRNs above (one per line), then click Apply." 
                       Foreground="#E65100"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="4" Background="#E8E8E8" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Apply Filter" 
                        Click="Apply_Click"
                        Padding="15,8" Margin="5"
                        MinWidth="100"
                        Background="#2196F3" Foreground="White" FontWeight="SemiBold"/>
                <Button Content="Remove Filter" 
                        Click="RemoveFilter_Click"
                        Padding="15,8" Margin="5"
                        MinWidth="100"
                        ToolTip="Clear the MRN whitelist and show all patients"/>
                <Button Content="Cancel" 
                        Click="Cancel_Click"
                        Padding="15,8" Margin="5"
                        MinWidth="80"
                        IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
