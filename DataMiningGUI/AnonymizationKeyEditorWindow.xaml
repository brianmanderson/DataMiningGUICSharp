<Window x:Class="DataMiningGUI.AnonymizationKeyEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Anonymization Key Editor" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Anonymization Key Editor" FontSize="18" FontWeight="Bold" Foreground="#1976D2"/>
            <TextBlock Text="View and edit MRN to anonymized ID mappings" 
                       Foreground="#666666" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- File Location -->
        <Border Grid.Row="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" 
                CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Key File Location:" 
                           VerticalAlignment="Center" FontWeight="Medium" Margin="0,0,12,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="KeyFilePathTextBox" 
                         VerticalAlignment="Center" Padding="8,6" IsReadOnly="True"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Change Folder..." 
                        Click="ChangeFolderButton_Click" Padding="12,6" Margin="8,0,0,0"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" 
                           x:Name="FileStatusTextBlock" Margin="0,8,0,0" 
                           Foreground="#666666" FontSize="11"/>
            </Grid>
        </Border>

        <!-- Mappings DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" 
                BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#EEEEEE" 
                        BorderThickness="0,0,0,1" Padding="12,8">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="+ Add Mapping" Click="AddMappingButton_Click" 
                                Padding="12,6" Margin="0,0,8,0" Background="#4CAF50" Foreground="White"/>
                        <Button Content="− Delete Selected" Click="DeleteMappingButton_Click" 
                                Padding="12,6" Background="#F44336" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" x:Name="MappingsDataGrid" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="32"
                          Margin="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Original MRN" Binding="{Binding OriginalMRN}" 
                                            Width="*" IsReadOnly="False">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Anonymized ID" Binding="{Binding AnonymizedID}" 
                                            Width="*" IsReadOnly="False">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Cancel" Click="CancelButton_Click"
                    Padding="16,8" Width="120" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="Save" Click="SaveButton_Click"
                    Padding="16,8" Width="120" Background="#1976D2" Foreground="White"/>
        </Grid>
    </Grid>
</Window>